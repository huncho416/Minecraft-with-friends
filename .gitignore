# Build output
target/
**/target/
*.class
*.jar
*.war

# Project-local toolchains (Maven, JDK, Rust were extracted here for ad-hoc builds)
.build-tools/

# IDE / editor
.idea/
*.iml
.settings/
.classpath
.project
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Docker VPS runtime server roots
tools/docker/servers/

# Local toolchain artefacts
.tools/
.vendor-log.txt

# Rust
**/Cargo.lock.bak

# NOTE: mythic-cord/infrarust/ is intentionally NOT ignored — the vendored
# subtree is committed so fresh clones and CI build without re-running the
# vendor script. Re-baselining is `tools/vendor-infrarust.{sh,ps1} <ref>`.
